/***
 *Component Communication
    -- Binding
        -- one way Binding
            -- Interpolation
            -- attribute Binding
        -- two way Binding  
            -- normal way
            -- formsModule
    --Event Binding (How To fire Event in Angular)


 == Directives  
    -- Structure Directives
        *ngIf -- *ngSwitch -- *ngFor
    -- Component Directives
    -- Attribute Directives
        ngClass -- ngStyle -- class.className -- style
 *
 */
